# Benchmark baselines - changelog (D9 §5.11)

One line per baseline change: the WP, the metric, old → new, why. A metric outside
tolerance is fixed or explained here; a tolerance is never loosened.

| WP | build | tier | change |
|---|---|---|---|
| WP4.3c | `7b93970` + WP4.3c | smoke | First baseline: `us_demand@1` × `y2026_27@1`, every load controlled - SRP's summer 30-minute on-peak demand (14:00–20:00 weekdays, May–October, three bands; D-0644), Phoenix normals, split 240 V, the heat pump cooling at 24 °C, USD, `SPOT_LIKE` reused. Two weeks: fee 31.05 USD (2.61 kW, October; January is outside SRP's season), `comfort_violation_min` 0, `deadline_misses` 0, `sessions_dropped` 10, `commitment_breaks` 0, `over_target` 18 (off-peak windows over the auto target, outside the billed window), `writes` 2808, `cost_energy` 118.93 USD, `savings` 26.27 USD, 410 ticks/s. |
| WP4.3b | `835e0b2` + WP4.3b | smoke | First baseline: `es_contracted@1` × `y2026_27@1`, every load controlled - `es/2_0td` filled P1 5.75 kW, P2 9.2 kW (a trip each), Madrid, TT 400 V, EUR, `SPOT_LIKE` reused (D-0641). Two weeks: `over_target` 0, max 6.61 kWh (October) and 7.66 kWh (January, a P2 night), fee 0.00 EUR (a trip limit bills nothing), `comfort_violation_min` 10.67, `deadline_misses` 10, `sessions_dropped` 10, `commitment_breaks` 0, `writes` 2935, `cost_energy` 164.97 EUR, `savings` 12.48 EUR, 410 ticks/s. |
| WP4.3b | `835e0b2` + WP4.3b | smoke | First baseline: `fr_tempo@1` × `y2026_27@1`, every load controlled - `fr/kva` filled 12 kVA, Paris, TT 400 V, EUR; the energy price is EDF's Tempo grid through the `day_type` add-on, the colours from `TempoSim` announced at 10:40 the day before (D-0641). Two weeks: `over_target` 0, max 8.99/9.44 kWh, fee 0.00 EUR, `comfort_violation_min` 11.00, `deadline_misses` 0, `sessions_dropped` 9, `commitment_breaks` 0, `writes` 2466, `cost_energy` 138.67 EUR, `savings` 7.28 EUR, 415 ticks/s. |
| WP4.3b | `835e0b2` + WP4.3b | smoke | First baseline: `fi_linear@1` × `y2026_27@1`, every load controlled - the tehomaksu shape (8 kW free on the month's highest hour, 2.50 EUR/kW/month, D-0641), Helsinki, EUR, `SPOT_LIKE` reused. Two weeks: 336 hourly windows, `over_target` 0, max 8.82 kWh (January), fee 2.05 EUR, `comfort_violation_min` 11.67, `deadline_misses` 3, `sessions_dropped` 10, `commitment_breaks` 3 (recorded as the house's starting value), `writes` 3311, `cost_energy` 189.61 EUR, `savings` 51.06 EUR, 393 ticks/s. |
| WP4.6 | `6de8246` + WP4.6 | smoke | `nordic_detached`: the preset is `no/tensio-ts` with its verified versions plus the fixture's labelled synthetic 2027-01-01 version (D-0524, dec. 21). `total.fee` 857.00 → 818.00 NOK: both months stay in the 5–10 kW step, now billed at Tensio TS's own 397 NOK (tensio.no) in October and the fixture's 421 NOK (397 × 1.06) in January, where the retired file had 416 and 441. Every control metric unchanged except `total.writes` 2734 → 2736 (within 10 %); `tick_p95_ms` 3.473 → 3.712, `plan_p95_ms` 51.552 → 52.183 (noise, within tolerance). 432 ticks/s. |
| WP4.6 | `6de8246` + WP4.6 | smoke | `be_quarter`: the tariff is `be/fluvius-imewo` (54.2009816 EUR/kW/year excl. VAT, VREG's Imewo 2026 sheet; was the regional average 53.39, D-0527). `total.fee` 88.76 → 93.11 EUR (a `not_worse` breach, explained here): the rate alone makes it 88.76 × 54.2010 / 53.39 = 90.11; the other 3.00 EUR, like `comfort_violation_min` 15.67 → 7.67, `deadline_misses` 8 → 2, `cost_energy` 244.34 → 190.80 EUR, `savings` 116.96 → 122.85 EUR and `writes` 3603 → 3029, comes from the house's changes since this baseline was set at WP4.3 (`a8451b3`) - D-0384's halved tank draw among them - which were never re-run for this house; not separated in this WP. `over_target` 1, `sessions_dropped` 10 → 9. 418 ticks/s. |
| WP4.3 | `a8451b3` | smoke | First baseline: `be_quarter@1` × `y2026_27@1`, D9 §5.9's third house, every load controlled - `be/fluvius`'s rolling-12, quarter-hour capacity tariff, EUR, `SPOT_LIKE` reused for the energy price (D-0311, D-0312). Two weeks: 1344 quarter-hour windows (four Tensio's own count), `over_target` 1, `comfort_violation_min` 15.67 min, `deadline_misses` 8, `sessions_dropped` 10, `writes` 3603, fee 88.76 EUR (`min_kw = 2.5`'s own floor visibly binding - the peak warning log shows it firing at 2.49/2.50 kWh), `cost_energy` 244.34 EUR, `savings` 116.96 EUR, 149 ticks/s. Zero-class tolerances `not_worse`/`zero` per §5.11, same as every house's own first baseline. |
| WP4.3 | `a8451b3` | smoke | First baseline: `nl_pv@1` × `y2026_27@1`, D9 §5.9's second house, every load controlled - `nl/connection`'s hard 17.25 kW `ContractedPower` trip, EUR, a new `SOLAR_GLUT` duck-curve energy price and a new 6 kWp `ProductionSim` roof (D-0310, D-0311). Two weeks: 336 hourly windows, `over_target` 1, `comfort_violation_min` 4.5 min, `deadline_misses` 10, `sessions_dropped` 9, `writes` 2932, fee 0.00 EUR every month (a trip limit, not a capacity fee - nothing to bill), `cost_energy` 70.24 EUR, `savings` 10.18 EUR, 189 ticks/s. Zero-class tolerances `not_worse`/`zero` per §5.11. |
| WP5.6 | `07fc4f2` | smoke | `tank.py`, `schedule.py`, `idle.py` built (D11 §9 6, 8, 9); `nordic_detached` has only a tank among the three kinds. Building it surfaced a simulator bug, fixed in the same PR (D-0384): `tests/sim/tank.py::DrawProfile.litres_at_55` read each local day's draws twice, so the tank drew double the household's D4 §5.7 profile - halving it moves the house's own tank energy and, with it, every money and control column at once, exactly as D-0384 predicted. `total.comfort_violation_min` 12.000 → 4.830 and `total.deadline_misses` 3 → 0 (less tank draw leaves the radiators more capacity under the priority walk); `total.writes` 2880 → 2734 (within the 10 % tolerance); `total.sessions_dropped` 9 → 10 (`not_worse`, one more BLE drop-while-paused from the EV's shifted write pattern - the simulator's own noise, not a regression, carried forward as the new baseline). `total.kwh` 1082.86 → 911.72, `total.cost_energy` 721.33 → 572.67 NOK, `total.cost_counterfactual` 2425.76 → 2313.91 NOK, `total.savings` 847.50 → 884.32 NOK (the tank's own savings enter the total for the first time - until this WP `shadow_for(TANK)` was `None` and its savings were unstated - on top of the halved draw); `total.fee` unchanged at 857.00 NOK (the capacity step does not move). `cost_energy`/`savings` are not among this baseline file's own tracked tolerances (a gap predating this WP - D-0267/D-0268's default `not_worse`/`not_less` were never carried into `nordic_detached.json`'s `tolerances` block); reported here for the record, not gated. 234 ticks/s. |
|---|---|---|---|
| WP3.6 | `784b55b` | smoke | `total.comfort_violation_min` 10.500 → 12.000. Two movements land in this one update, neither a regression this WP introduced by accident: (1) **backfilled from D-0277** (`design/DECISIONS.md`): the register-repeat-frame fix changed which windows close on the integral, and D-0277's own text called for "a baseline update with a changelog line, not a tolerance change" at the time - that update was never made, so every checkpoint from WP2.5 through WP3.5 carried a `10.500 → 10.670` breach in its own transcript, verbally attributed to D-0277 and left unresolved rather than recorded; this line closes that gap. (2) **WP3.6 itself**, 10.670 → 12.000: `Engine._cycle_reservations` makes `CycleReservation` real for the first time (D-0305) - the dishwasher's ~300 W profile power is now genuinely held out of the priority walk's trim while its cycle runs, where before the reservation was unconstructed dead code and the dishwasher competed for capacity like any other load. Diagnosed by re-running the smoke window's January span against the WP3.5 commit in a scratch worktree with the same load-level observer: `comfort_violation_min` over that span reads 9.83 min at `5ba84e1` and 10.17 min at `784b55b`, both runs showing the same violating loads (`radiator_bed_1`/`radiator_bed_2`, the reference house's tightest comfort margins, 17.0 °C floor with a 0.2 K swing) and the same pre-existing pattern of frequent short dishwasher runs (present identically at both commits, so not a WP3.6 artifact) - the now-protected block's power is simply no longer available for the radiators to compete for during those windows. `total.writes` 2713 → 2880 (within the 10 % tolerance, the `follow_presence`/schedule-hydration entities added since WP3.5) and `total.sessions_dropped` 10 → 9 (improved) also move but stay `ok`; every other metric is unchanged. |
| WP0.11 | `801993f` | smoke | First baseline: `nordic_detached@1` × `y2026_27@1`, every load controlled (D-0264). Two weeks: 334 windows, `over_target` 0, `comfort_violation_min` 10.5 (bedroom heaters at the floor under stage-2 sheds), `deadline_misses` 3 (recorded, `not_worse` until WP2.6), `sessions_dropped` 10 (the BLE simulator's own drops while paused), `writes` 2 713, fee 857 NOK, 225 ticks/s (§5.9 asks 500; D-0261). Zero-class tolerances start as `not_worse` per §5.11; `engine_failures`, `zero_amp_writes`, `commitment_breaks`, `plan_gaps` are `zero` from the start. Money columns from D11 wait for WP0.10. |
| WP0.10 | `ad8e77d` | smoke | House spec `nordic_detached@1` → `@2` (D-0268: the floor loops answer `loss_coeff_w_per_k` at 0.7 W/m²K × area, the car carries its 80 % app limit) - a reset that moves **no** control metric: windows, `over_target` 0, comfort 10.5 min, `deadline_misses` 3, writes 2 713, fee 857 NOK all identical to `801993f`. New money columns from the D11 ledger behind D7 (D-0267): `cost_energy` 721.78 NOK, `cost_counterfactual` 2 422.20 NOK, `savings` 843.50 NOK over the two weeks (October 427, January 416 - capacity dominates: the uncontrolled car at plug-in lands the counterfactual two steps above the controlled 5–10 kW); tolerances `cost_energy` `not_worse`, `savings` `not_less`. 198 ticks/s measured alongside other suites (225 alone at `801993f`; the per-load meters and the slot closes are the added tick work). |
